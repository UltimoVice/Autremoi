import { Link } from "react-router-dom";

export function Cards(title,name,price){
    return(
        <div>
            <img>{images}</img>
            <h4>{title}</h4>
            <p>{price}</p>
        </div>
    )
        

    
}